// This file provides generateStaticParams for static export.
// The actual page is fully client-side rendered via 'use client'.
export function generateStaticParams() {
  // Return empty — the page is rendered client-side at runtime
  return [];
}
